# Frontend runtime config. Copy to `.env` and adjust for your environment.
# Both default to the local dev backend when unset.

# REST API base URL (no trailing slash).
VITE_API_BASE=http://localhost:8000

# WebSocket URL for the live sensor stream.
VITE_WS_URL=ws://localhost:8000/ws/sensors
